{
  "name": "its-forms",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Internal online forms + approval workflow system (replaces paper MEMO)",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node --env-file=.env src/server.js",
    "dev": "node --watch --env-file=.env src/server.js",
    "test": "node --env-file=.env --test test/"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "cookie-session": "^2.1.0",
    "ejs": "^3.1.10",
    "express": "^4.21.0",
    "mongodb": "^6.9.0",
    "multer": "^2.4.0"
  }
}
